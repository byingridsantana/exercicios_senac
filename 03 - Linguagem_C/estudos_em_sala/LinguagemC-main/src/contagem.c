#include <stdio.h>

int main(){
    int valor;                              // variavel 
    valor = 10;
    while(valor >= 1){                      //enquanto a variavel for maior que
        printf("%d\n",valor);               //imprime
        valor--;                            //incrementar 
    }

    return 0;
}